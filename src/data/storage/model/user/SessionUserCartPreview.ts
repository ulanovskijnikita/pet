export type SessionUserActiveCartId = string
export type SessionUserCartLength = string

export default interface SessionUserCartPreview {

    id: SessionUserActiveCartId
    length: SessionUserCartLength
}